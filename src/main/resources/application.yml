spring:
  application:
    name: ekumen-eureka-server
server:
  port: 8761

# eureka server configuration
eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
  instance:
    hostname: ekumen-eureka-server
  server:
    renewal-percent-threshold: 0.90


